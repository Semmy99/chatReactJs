import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  typography: {
    margin: 10,
    width: '100%',
    color: '#18c83e;',
  },
  myMessage: {
    textAlign: 'right',
    color: 'blue',
    width: '100%',
  },
}));
